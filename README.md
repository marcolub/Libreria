## Simple bookstore Django and Vue.js

Activate pipenv and install dependencies, then :

```bash
python manage.py makemigrations
python manage.py migrate
```

Then :

```bash
yarn build
python manage.py runserver
```